---
# Image Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-service
  namespace: astria-bot
  labels:
    app: image-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: image-service
  template:
    metadata:
      labels:
        app: image-service
    spec:
      containers:
      - name: image-service
        image: acryourregistry.azurecr.io/astria-bot/image-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: astria-secrets
              key: database_url
        - name: SERVICEBUS_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: astria-secrets
              key: servicebus_connection_string
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"

---
# Image Service Service
apiVersion: v1
kind: Service
metadata:
  name: image-service
  namespace: astria-bot
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: image-service
